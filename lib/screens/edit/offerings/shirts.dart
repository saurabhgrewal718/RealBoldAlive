import 'package:flutter/material.dart';

class Shirts extends StatelessWidget {
 
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(
        child: Text('this i sthe Shirts file'),
      )
    );
  }
}