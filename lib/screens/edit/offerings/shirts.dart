import 'package:flutter/material.dart';

class Shirts extends StatelessWidget {
 static const routeName= './shirts';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Container(
        child:Center(
          child: Text('this i sthe Shirts file'),
        )
      ),
    );
  }
}