import 'package:flutter/material.dart';

class Mugs extends StatelessWidget {
  static const routeName= './mugs';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Container(
        child:Center(
          child: Text('this i sthe Mugs file'),
        )
      ),
    );
  }
}