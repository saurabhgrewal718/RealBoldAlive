import 'package:flutter/material.dart';

class Mugs extends StatelessWidget {
 
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(
        child: Text('this is the Mugs file'),
      )
    );
  }
}